<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.dasi.infrastructure.persistent.dao.IUserDao">

    <resultMap id="dataMap" type="com.dasi.infrastructure.persistent.po.User">
        <id     property="id"        column="id"/>
        <result property="userId"    column="user_id"/>
        <result property="password"  column="password"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="queryUserByUserId" parameterType="java.lang.String" resultMap="dataMap">
        SELECT user_id, password
        FROM user
        WHERE user_id = #{userId};
    </select>

    <insert id="insertUser" parameterType="com.dasi.infrastructure.persistent.po.User">
        INSERT INTO user (user_id, password)
        VALUES (#{userId}, #{password});
    </insert>

</mapper>
