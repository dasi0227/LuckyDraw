<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dasi.infrastructure.persistent.dao.IAwardDao">

    <resultMap id="dataMap" type="com.dasi.infrastructure.persistent.po.Award">
        <id     column="id"           property="id"/>
        <result column="award_id"     property="awardId"/>
        <result column="award_name"    property="awardName"/>
        <result column="award_config" property="awardConfig"/>
        <result column="award_desc"   property="awardDesc"/>
        <result column="create_time"  property="createTime"/>
        <result column="update_time"  property="updateTime"/>
    </resultMap>

    <select id="queryAwardList" resultMap="dataMap">
        select award_id, award_name, award_config, award_desc
        from award
    </select>

    <select id="queryAwardByAwardId" parameterType="java.lang.Long" resultMap="dataMap">
        select award_id, award_name, award_config, award_desc
        from award
        where award_id = #{awardId}
    </select>

</mapper>
